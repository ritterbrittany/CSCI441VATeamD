# Enable mod_rewrite
RewriteEngine On

# Set the Directory Index to login.php (default page)
DirectoryIndex login.php

# Optionally, allow pretty URLs
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ /backend/$1.php [L]
